---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/components/Map/MapView.tsx
  - src/hooks/useTerradraw.ts
  - src/components/Sidebar/Sidebar.tsx
  - src/components/Sidebar/SelectionInfo.tsx
  - src/components/Sidebar/GenerateButton.tsx
autonomous: true
requirements:
  - LOCS-02
  - LOCS-03

must_haves:
  truths:
    - "User can draw a bounding box on the map by clicking and dragging"
    - "User can resize the bounding box by dragging its edges or corners"
    - "User can reposition the bounding box by dragging its interior"
    - "Sidebar displays real-world dimensions (e.g., '2.3 km x 1.8 km') and corner coordinates when a bbox is selected"
    - "Generate Preview button is visible but disabled"
  artifacts:
    - path: "src/hooks/useTerradraw.ts"
      provides: "Terra Draw lifecycle management with rectangle and select modes"
      min_lines: 40
    - path: "src/components/Sidebar/SelectionInfo.tsx"
      provides: "Bbox dimension and coordinate display"
      min_lines: 25
    - path: "src/components/Sidebar/GenerateButton.tsx"
      provides: "Disabled generate preview button"
      min_lines: 8
  key_links:
    - from: "src/hooks/useTerradraw.ts"
      to: "src/store/mapStore.ts"
      via: "onChange handler calls setBbox with extracted WGS84 coordinates"
      pattern: "setBbox"
    - from: "src/components/Sidebar/SelectionInfo.tsx"
      to: "src/store/mapStore.ts"
      via: "useMapStore selector reads bbox dimensions"
      pattern: "useMapStore"
    - from: "src/components/Map/MapView.tsx"
      to: "src/hooks/useTerradraw.ts"
      via: "useTerradraw hook called with map instance"
      pattern: "useTerradraw"
---

<objective>
Add bounding box drawing and editing to the map using Terra Draw, wire bbox coordinates through the Zustand store with UTM projection, and display selection info (real-world dimensions, corner coordinates, area warning) in the sidebar with a disabled Generate Preview button.

Purpose: Complete the user-facing selection workflow — users can find a location (Plan 01), draw a precise bounding box, adjust it, and see exactly what they've selected before proceeding to 3D generation.

Output: Fully interactive bbox selection with sidebar feedback showing real-world dimensions in km and corner coordinates.
</objective>

<execution_context>
@/Users/agrignon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/agrignon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Terra Draw with rectangle and select modes, wire bbox to store</name>
  <files>
    src/hooks/useTerradraw.ts
    src/components/Map/MapView.tsx
  </files>
  <action>
    Create `src/hooks/useTerradraw.ts`:
    - Custom React hook: `useTerradraw(map: maplibregl.Map | null)`
    - When map is available (non-null), create a TerraDraw instance with:
      - `TerraDrawMapLibreGLAdapter` adapter (import from `terra-draw-maplibre-gl-adapter`; if package not found separately, try importing from `terra-draw` directly — see open question in research)
      - `TerraDrawRectangleMode()` for drawing rectangles
      - `TerraDrawSelectMode` configured with:
        ```
        flags: {
          rectangle: {
            feature: {
              draggable: true,           // drag interior to reposition
              coordinates: {
                midpoints: true,         // edge midpoint handles
                draggable: true,         // corners/edges draggable
                resizable: 'opposite',   // corner-based scaling
              },
            },
          },
        }
        ```
    - Call `draw.start()` once after construction
    - Set initial mode to `'rectangle'` via `draw.setMode('rectangle')`
    - Listen to `draw.on('change', (ids, type) => { ... })`:
      - On 'create' or 'update': call `draw.getSnapshot()`, filter for polygon features with mode === 'rectangle'
      - Extract the ring of coordinates from the first rectangle feature
      - Compute SW (min lon, min lat) and NE (max lon, max lat)
      - Call `useMapStore.getState().setBbox({ lon: swLon, lat: swLat }, { lon: neLon, lat: neLat })`
    - After a rectangle is drawn (on 'create'), automatically switch mode to `'select'` so the user can immediately resize/reposition without a manual mode toggle
    - On 'delete' (if user deletes the rectangle): call `useMapStore.getState().clearBbox()`
    - Style the rectangle: semi-transparent fill (e.g., `rgba(59, 130, 246, 0.2)` — blue-200 with 20% opacity), solid border (e.g., `rgba(59, 130, 246, 0.8)` — blue-500 with 80% opacity, 2px width). Check Terra Draw's styling API — styles may be set via mode options or via the adapter's style configuration.
    - Limit to one rectangle at a time: before drawing a new rectangle, remove any existing rectangles from the snapshot using `draw.removeFeatures(existingIds)` when a new 'create' event fires for a second rectangle.
    - Cleanup: return a cleanup function that calls `draw.stop()` on unmount
    - Store the TerraDraw instance in a ref so it persists across renders

    Modify `src/components/Map/MapView.tsx`:
    - Import `useMap` from `@vis.gl/react-maplibre`
    - Inside the Map component's child, get the map instance: `const { 'main-map': mapRef } = useMap()`
    - Call `useTerradraw(mapRef?.getMap() ?? null)` to initialize Terra Draw when the map is ready
    - Ensure the hook is called inside a child component of `<Map>`, not alongside it (useMap requires Map context)
    - If MapView currently renders SearchOverlay as a direct child of Map, add another child component (e.g., `MapInteractions`) that handles both Terra Draw init and any future map interactions

    Per user's locked decision:
    - Click-and-drag to draw (TerraDrawRectangleMode default behavior)
    - Free aspect ratio (no constraints — TerraDrawRectangleMode default)
    - Semi-transparent colored fill with solid border (styled as above)
    - After placement: drag edges/corners to resize, drag interior to reposition (TerraDrawSelectMode)
    - Area outside selection could be dimmed — implement a simple dimming layer if straightforward: add a MapLibre fill layer that covers the entire visible area with a semi-transparent dark overlay, then use the bbox polygon as a "hole" to cut out the selected area. If this is complex to wire with Terra Draw's dynamic geometry, defer dimming to a follow-up and note it in the summary.
  </action>
  <verify>
    <automated>cd /Users/agrignon/projects/MapMaker && npx tsc --noEmit 2>&1 | tail -10</automated>
    <manual>Run dev server, click and drag on map to draw a rectangle. Verify: blue semi-transparent rectangle appears, releasing the mouse switches to select mode, dragging a corner resizes, dragging interior repositions, drawing a new rectangle replaces the old one.</manual>
  </verify>
  <done>User can click-and-drag to draw a bounding box rectangle on the map. After drawing, user can resize via corners/edges and reposition by dragging the interior. Only one rectangle exists at a time. Bbox coordinates flow to the Zustand store on every change.</done>
</task>

<task type="auto">
  <name>Task 2: Display selection info in sidebar with dimensions, coordinates, and generate button</name>
  <files>
    src/components/Sidebar/SelectionInfo.tsx
    src/components/Sidebar/GenerateButton.tsx
    src/components/Sidebar/Sidebar.tsx
  </files>
  <action>
    Create `src/components/Sidebar/SelectionInfo.tsx`:
    - Subscribe to `useMapStore` for `bbox`, `dimensions`, and `utmZone`
    - If no bbox selected, show nothing (or the placeholder instruction text)
    - When bbox is selected, display:
      - **Real-world dimensions** formatted as "X.X km x Y.Y km" (convert meters to km, 1 decimal place). For very small areas, show meters instead (e.g., "450 m x 320 m" if < 1km). Use a simple threshold: if either dimension < 1000m, show meters; otherwise show km.
      - **Corner coordinates**: SW and NE in lat/lon format with 4 decimal places (e.g., "SW: 40.7128, -74.0060")
      - **UTM zone**: Display the computed UTM zone (e.g., "UTM Zone 18N")
      - **Area warning**: If either dimension exceeds 5 km, show a soft warning: "Large area selected — processing may take longer." Style as a yellow/amber inline note, non-blocking.
    - Style with Tailwind: clean typography, subtle borders between sections, dark mode variants

    Create `src/components/Sidebar/GenerateButton.tsx`:
    - "Generate Preview" button
    - Always disabled in Phase 1 (add `disabled` attribute and `cursor-not-allowed opacity-50` styles)
    - Styled as a primary action button: full-width, blue background (`bg-blue-600`), white text
    - When disabled: show tooltip or subtitle text "Available after Phase 2" or simply gray it out
    - Subscribe to `useMapStore` to check if bbox exists — button text could show "Select an area first" when no bbox, "Generate Preview" when bbox exists (but still disabled)

    Modify `src/components/Sidebar/Sidebar.tsx`:
    - Import and render `<SelectionInfo />` and `<GenerateButton />`
    - Layout: title at top, SelectionInfo in the middle (or instruction text if no selection), GenerateButton at the bottom (pinned with flex layout)
    - Sidebar structure:
      ```
      <aside className="w-[300px] h-screen flex flex-col ...">
        <div className="p-4 border-b">MapMaker title</div>
        <div className="flex-1 overflow-y-auto p-4">
          <SelectionInfo />
        </div>
        <div className="p-4 border-t">
          <GenerateButton />
        </div>
      </aside>
      ```
    - Keep the instruction placeholder text from Plan 01 but conditionally show it only when no bbox is selected
  </action>
  <verify>
    <automated>cd /Users/agrignon/projects/MapMaker && npx tsc --noEmit && npx vitest run src/lib/__tests__/ 2>&1</automated>
    <manual>Run dev server, draw a bounding box on the map. Verify: sidebar updates to show real-world dimensions in km (e.g., "2.3 km x 1.8 km"), corner coordinates with 4 decimal places, UTM zone. Draw a large box (>5km) and verify the area warning appears. Verify Generate Preview button is visible but disabled.</manual>
  </verify>
  <done>Sidebar displays real-world bbox dimensions (km or m), corner coordinates, UTM zone, and area warning for large selections. Generate Preview button is visible but disabled. All info updates reactively when bbox is resized or repositioned. All existing tests still pass.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — no TypeScript errors
2. `npx vitest run src/lib/__tests__/` — all UTM/STL tests still pass
3. User can draw a rectangle on the map by clicking and dragging
4. User can resize the rectangle by dragging corners or edges
5. User can reposition the rectangle by dragging its interior
6. Sidebar shows real-world dimensions and coordinates when bbox is selected
7. Area warning appears for selections > 5km in either dimension
8. Generate Preview button is visible but disabled
</verification>

<success_criteria>
- Bounding box can be drawn via click-and-drag on the satellite map
- Bounding box can be resized by dragging corners/edges and repositioned by dragging interior
- Sidebar reactively displays: dimensions in km/m, corner coordinates, UTM zone
- Large area warning appears when appropriate
- Generate Preview button is present and disabled
- Only one bounding box exists at a time (drawing a new one replaces the old)
- All automated tests from Plan 01 continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
