---
phase: 03-buildings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/buildings/overpass.ts
  - src/lib/buildings/parse.ts
  - src/lib/buildings/height.ts
  - src/lib/buildings/elevationSampler.ts
  - src/lib/buildings/footprint.ts
  - src/lib/buildings/walls.ts
  - src/lib/buildings/roof.ts
  - src/lib/buildings/buildingMesh.ts
  - src/lib/buildings/merge.ts
  - src/lib/buildings/types.ts
  - src/lib/buildings/__tests__/height.test.ts
  - src/lib/buildings/__tests__/elevationSampler.test.ts
  - src/lib/buildings/__tests__/footprint.test.ts
autonomous: true
requirements:
  - BLDG-01
  - BLDG-03
  - BLDG-04

must_haves:
  truths:
    - "Overpass API returns building data for a known bbox with buildings"
    - "Height resolver never returns undefined/NaN — always produces a positive number"
    - "Height fallback cascade applies in order: height tag, levels*3.5, type default"
    - "Elevation sampler returns correct interpolated terrain Z for any lon/lat within bbox"
    - "Building geometry is a closed solid (floor cap + walls + roof cap) for a simple rectangular footprint"
  artifacts:
    - path: "src/lib/buildings/overpass.ts"
      provides: "Overpass API building data fetcher"
      exports: ["fetchBuildingData"]
    - path: "src/lib/buildings/parse.ts"
      provides: "osmtogeojson conversion and feature extraction"
      exports: ["parseBuildingFeatures"]
    - path: "src/lib/buildings/height.ts"
      provides: "Height fallback hierarchy resolver"
      exports: ["resolveHeight"]
    - path: "src/lib/buildings/elevationSampler.ts"
      provides: "Bilinear terrain elevation sampling at lon/lat"
      exports: ["sampleElevationAtLonLat"]
    - path: "src/lib/buildings/footprint.ts"
      provides: "Earcut polygon triangulation for building footprints"
      exports: ["triangulateFootprint"]
    - path: "src/lib/buildings/walls.ts"
      provides: "Per-vertex base elevation wall construction"
      exports: ["buildWalls"]
    - path: "src/lib/buildings/roof.ts"
      provides: "Flat roof cap geometry"
      exports: ["buildFlatRoof"]
    - path: "src/lib/buildings/buildingMesh.ts"
      provides: "Single building BufferGeometry composer"
      exports: ["buildSingleBuilding"]
    - path: "src/lib/buildings/merge.ts"
      provides: "Merge all buildings + coordinate pipeline orchestrator"
      exports: ["buildAllBuildings"]
    - path: "src/lib/buildings/types.ts"
      provides: "Building pipeline type definitions"
      exports: ["BuildingFeature", "ParsedBuilding"]
  key_links:
    - from: "src/lib/buildings/overpass.ts"
      to: "Overpass API"
      via: "HTTP POST to overpass-api.de"
      pattern: "fetch.*overpass"
    - from: "src/lib/buildings/parse.ts"
      to: "osmtogeojson"
      via: "osmtogeojson(overpassData)"
      pattern: "osmtogeojson"
    - from: "src/lib/buildings/elevationSampler.ts"
      to: "ElevationData"
      via: "bilinear interpolation of elevations grid"
      pattern: "elevations\\[.*gridSize"
    - from: "src/lib/buildings/buildingMesh.ts"
      to: "src/lib/buildings/walls.ts"
      via: "buildWalls(ring, baseZmm, heightMM)"
      pattern: "buildWalls"
    - from: "src/lib/buildings/merge.ts"
      to: "src/lib/utm.ts"
      via: "wgs84ToUTM for coordinate projection"
      pattern: "wgs84ToUTM"
---

<objective>
Build the complete building data and geometry pipeline — from Overpass API fetch through to merged Three.js BufferGeometry.

Purpose: This plan creates the core library that all subsequent plans depend on. Every building-related function (fetch, parse, height resolve, elevation sample, triangulate, extrude walls, cap roof, merge) is built and unit-tested here. No UI integration yet — pure library code with full test coverage.

Output: 10 new library files in src/lib/buildings/, 3+ test files, npm dependencies installed
</objective>

<execution_context>
@/Users/agrignon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/agrignon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-buildings/03-RESEARCH.md

Key existing files to reference:
@src/types/geo.ts (ElevationData, BoundingBox types)
@src/lib/utm.ts (wgs84ToUTM, getUTMZone, bboxDimensionsMeters)
@src/lib/mesh/terrain.ts (buildTerrainGeometry coordinate conventions — Y-axis: vy=0=north=+Y, mesh centered at origin)
@src/store/mapStore.ts (existing store shape)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, create building types, Overpass fetcher, GeoJSON parser, and height resolver with tests</name>
  <files>
    package.json
    src/lib/buildings/types.ts
    src/lib/buildings/overpass.ts
    src/lib/buildings/parse.ts
    src/lib/buildings/height.ts
    src/lib/buildings/__tests__/height.test.ts
  </files>
  <action>
1. Install dependencies:
   ```bash
   npm install earcut osmtogeojson three-bvh-csg three-mesh-bvh
   npm install -D @types/earcut @types/osmtogeojson
   ```
   Note: `three@0.183.1` already installed — satisfies `three-bvh-csg` peer dep `>= 0.179.0`. If `@types/osmtogeojson` does not exist on npm, create a local declaration file `src/types/osmtogeojson.d.ts` with `declare module 'osmtogeojson'`.

2. Create `src/lib/buildings/types.ts`:
   - `BuildingFeature`: Parsed building with properties (height?, building:levels?, roof:shape?, building type), geometry coordinates (outer ring + holes as [lon, lat][] arrays)
   - `ParsedBuilding`: BuildingFeature + resolvedHeightM (number), roofShape (string), roofHeightM (number)
   - `BuildingGeometryParams`: widthMM, depthMM, geographicWidthM, geographicDepthM, utmZone (number), bboxCenterUTM ({x, y}) — reusable across all building geometry functions

3. Create `src/lib/buildings/overpass.ts`:
   - `fetchBuildingData(bbox: BoundingBox): Promise<unknown>` — POST to `https://overpass-api.de/api/interpreter`
   - Query format: `[out:json][timeout:60][maxsize:33554432][bbox:${sw.lat},${sw.lon},${ne.lat},${ne.lon}]; (way["building"]; way["building:part"]; relation["building"]["type"="multipolygon"];); out geom;`
   - CRITICAL: Overpass bbox order is `south,west,north,east` = `sw.lat,sw.lon,ne.lat,ne.lon` — NOT the GeoJSON order
   - Return the raw JSON response. Throw on non-200 status.

4. Create `src/lib/buildings/parse.ts`:
   - `parseBuildingFeatures(overpassData: unknown): BuildingFeature[]`
   - Use `osmtogeojson(overpassData)` to convert to GeoJSON FeatureCollection
   - For each feature with `geometry.type === 'Polygon'`: extract outer ring (coordinates[0]) and holes (coordinates[1..n])
   - For `MultiPolygon`: extract each polygon separately as individual BuildingFeature entries
   - Extract from properties: `height`, `building:levels`, `roof:shape` (default 'flat'), `roof:height`, `building` (type tag)
   - Skip features with no coordinates or empty rings

5. Create `src/lib/buildings/height.ts`:
   - `resolveHeight(properties: Record<string, string | undefined>): number`
   - Cascade: (1) `parseFloat(properties.height)` if valid > 0, (2) `parseInt(properties['building:levels']) * 3.5` if valid > 0, (3) type defaults lookup by `properties.building` value
   - Type defaults map: house=7, residential=7, apartments=14, commercial=8, industrial=6, church=12, school=9, yes=7 (fallback)
   - Handle unit strings in height tag: `parseFloat("12 m")` = 12, `parseFloat("40'")` needs feet conversion (multiply by 0.3048)
   - `resolveRoofHeight(properties: Record<string, string | undefined>, wallHeight: number): number`
   - If `roof:height` tag exists, parseFloat it. Otherwise default: flat=0, gabled=wallHeight*0.3, hipped=wallHeight*0.3, pyramidal=wallHeight*0.4

6. Create `src/lib/buildings/__tests__/height.test.ts`:
   - Test resolveHeight with direct height tag: `{ height: '12' }` => 12
   - Test resolveHeight with height tag with units: `{ height: '12 m' }` => 12
   - Test resolveHeight with feet: `{ height: "40'" }` => ~12.19
   - Test resolveHeight with levels only: `{ 'building:levels': '4' }` => 14
   - Test resolveHeight with type default: `{ building: 'apartments' }` => 14
   - Test resolveHeight with generic building: `{ building: 'yes' }` => 7
   - Test resolveHeight cascade priority: `{ height: '20', 'building:levels': '3' }` => 20 (height wins)
   - Test resolveHeight with invalid height falls through: `{ height: 'abc', 'building:levels': '3' }` => 10.5
   - Test resolveRoofHeight defaults for each shape
  </action>
  <verify>
    <automated>cd /Users/agrignon/projects/MapMaker && npx vitest run src/lib/buildings/__tests__/height.test.ts --reporter=verbose 2>&1 | tail -30</automated>
    <manual>Verify earcut, osmtogeojson, three-bvh-csg, three-mesh-bvh appear in package.json dependencies</manual>
  </verify>
  <done>All height resolver tests pass. Dependencies installed. Overpass fetcher and GeoJSON parser modules exist with correct exports. Types defined.</done>
</task>

<task type="auto">
  <name>Task 2: Build elevation sampler, footprint triangulation, wall construction, flat roof, building mesh composer, and merge orchestrator with tests</name>
  <files>
    src/lib/buildings/elevationSampler.ts
    src/lib/buildings/footprint.ts
    src/lib/buildings/walls.ts
    src/lib/buildings/roof.ts
    src/lib/buildings/buildingMesh.ts
    src/lib/buildings/merge.ts
    src/lib/buildings/__tests__/elevationSampler.test.ts
    src/lib/buildings/__tests__/footprint.test.ts
  </files>
  <action>
1. Create `src/lib/buildings/elevationSampler.ts`:
   - `sampleElevationAtLonLat(lon, lat, bbox: BoundingBox, elevData: ElevationData): number`
   - Normalize lon/lat to [0,1] within bbox: `tx = (lon - sw.lon) / (ne.lon - sw.lon)`
   - CRITICAL Y-axis convention (matching terrain.ts): `ty = (ne.lat - lat) / (ne.lat - sw.lat)` — ne.lat is row 0 (north)
   - Compute grid coordinates: `gx = clamp(tx, 0, 1) * (gridSize - 1)`, `gy = clamp(ty, 0, 1) * (gridSize - 1)`
   - Bilinear interpolation of 4 surrounding grid cells
   - Return elevation in meters

2. Create `src/lib/buildings/__tests__/elevationSampler.test.ts`:
   - Test center of bbox returns center grid value
   - Test corners return corner grid values (no interpolation needed)
   - Test midpoint between two grid cells returns average
   - Test points outside bbox are clamped to edge
   - Use a simple 3x3 or 5x5 synthetic grid with known values

3. Create `src/lib/buildings/footprint.ts`:
   - `triangulateFootprint(outer: [number, number][], holes: [number, number][][]): { flatVertices: number[]; indices: number[] }`
   - Flatten outer ring vertices as [x0, y0, x1, y1, ...] (2D)
   - Record hole start indices
   - Call `earcut(flatVertices, holeIndices.length > 0 ? holeIndices : undefined, 2)`
   - Return flatVertices and triangle indices

4. Create `src/lib/buildings/__tests__/footprint.test.ts`:
   - Test simple square produces 2 triangles (6 indices)
   - Test L-shaped polygon produces correct number of triangles
   - Test square with hole produces correct triangulation (courtyard)
   - Verify all returned indices are within bounds of flatVertices

5. Create `src/lib/buildings/walls.ts`:
   - `buildWalls(ringXY: [number, number][], baseZmm: number[], buildingHeightMM: number): Float32Array`
   - For each consecutive pair of ring vertices, create a quad (2 triangles) connecting base to top
   - Each vertex has its own baseZ (per-vertex terrain elevation in mm) — this is how buildings follow slopes (BLDG-04)
   - Top Z = baseZ[i] + buildingHeightMM for vertex i
   - Winding: outward-facing CCW. Use the wall extrusion pattern from 03-RESEARCH.md Pattern 5.
   - IMPORTANT: Ring winding must produce outward normals. After UTM projection, OSM outer rings may be CW. Detect winding with signed area: if signed area > 0 (CW in screen space), reverse the ring before building walls.
   - Return Float32Array of positions (n_segments * 6 vertices * 3 coords)

6. Create `src/lib/buildings/roof.ts`:
   - `buildFlatRoof(topRingXY: [number, number][], topZ: number, holes: [number, number][][] = []): Float32Array`
   - Use `triangulateFootprint()` to get triangle indices for the top cap
   - Set all Z = topZ (for flat terrain base) or handle per-vertex topZ for slope: topZ[i] = baseZmm[i] + heightMM
   - Actually for flat roof: each vertex top Z = that vertex's baseZmm + buildingHeightMM. So accept `topZmm: number[]` (per-vertex) instead of a single topZ.
   - Return Float32Array of positions for the roof cap triangles
   - Also export `buildFloorCap(ringXY, baseZmm, holes)`: same as flat roof but at base elevation with reversed winding (normal points down)

7. Create `src/lib/buildings/buildingMesh.ts`:
   - `buildSingleBuilding(outerRingMM: [number, number][], holesMM: [number, number][][], baseZmm: number[], buildingHeightMM: number, roofShape: string, roofHeightMM: number): THREE.BufferGeometry`
   - Compose: floor cap (buildFloorCap) + walls (buildWalls) + roof (buildFlatRoof for now, gabled/hipped/pyramidal in Plan 02)
   - Merge into single BufferGeometry using `mergeGeometries`
   - This function takes coordinates already in mm local space — coordinate projection happens upstream

8. Create `src/lib/buildings/merge.ts`:
   - `buildAllBuildings(features: BuildingFeature[], bbox: BoundingBox, elevData: ElevationData, params: BuildingGeometryParams): THREE.BufferGeometry`
   - For each BuildingFeature:
     a. Resolve height in meters via `resolveHeight()`
     b. For each vertex in outer ring + holes: project [lon,lat] to UTM using `wgs84ToUTM()`, then to local mm space (subtract bbox center UTM, multiply by scale = widthMM/geographicWidthM)
     c. For each vertex: sample terrain elevation via `sampleElevationAtLonLat()`, convert to mm using same scale
     d. Convert building height from meters to mm: `heightMM = heightM * scale`
     e. Call `buildSingleBuilding()` for each building
   - Merge all individual building geometries using `mergeGeometries()`
   - The scale factor = widthMM / geographicWidthM (same as terrain.ts horizontalScale)
   - Center offset: bbox center in UTM = projectToUTM(centroidLon, centroidLat, utmZone). Each building vertex UTM coord becomes (utmX - centerX) * scale for X, (utmY - centerY) * scale for Y — matching the terrain mesh centering.
   - CRITICAL: The Y-axis convention must match terrain.ts — UTM Y increases north = mesh Y increases north = positive Y. This should be automatic since UTM northing increases north.
   - Return merged BufferGeometry or null if no buildings found
  </action>
  <verify>
    <automated>cd /Users/agrignon/projects/MapMaker && npx vitest run src/lib/buildings/__tests__/ --reporter=verbose 2>&1 | tail -40</automated>
    <manual>Check that all building module files export the expected functions</manual>
  </verify>
  <done>Elevation sampler tests pass with correct bilinear interpolation. Footprint triangulation tests pass for simple and complex polygons. Wall construction, flat roof, floor cap, building mesh composer, and merge orchestrator all exist with correct exports. All building library tests pass alongside existing terrain/elevation tests.</done>
</task>

</tasks>

<verification>
1. All tests pass: `npx vitest run` — both existing terrain/elevation tests and new building tests
2. No TypeScript errors: `npx tsc --noEmit`
3. Dependencies installed: earcut, osmtogeojson, three-bvh-csg, three-mesh-bvh in package.json
4. Height resolver never returns NaN or undefined for any input combination
5. Elevation sampler returns values within [minElevation, maxElevation] for any point within bbox
6. Building geometry from buildSingleBuilding has closed topology (floor + walls + roof)
</verification>

<success_criteria>
- 10+ new library files in src/lib/buildings/
- 3+ test files with 15+ passing tests
- Height fallback hierarchy verified by tests (BLDG-03)
- Bilinear elevation sampling verified by tests (BLDG-04)
- Footprint triangulation handles holes (courtyards)
- Wall construction uses per-vertex base elevation for slope correctness (BLDG-04)
- Coordinate pipeline matches terrain.ts conventions (Y-axis, centering, scale)
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-buildings/03-01-SUMMARY.md`
</output>
