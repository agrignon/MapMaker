---
phase: 15-content-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Panels/MapControlsPanel.tsx
  - src/components/Panels/ModelControlsPanel.tsx
  - src/components/Panels/SidebarContent.tsx
  - src/components/Sidebar/Sidebar.tsx
  - src/components/Preview/PreviewSidebar.tsx
autonomous: true
requirements: [LAYOUT-03]

must_haves:
  truths:
    - "Sidebar content switches from map controls to model controls when showPreview toggles"
    - "The same sidebar content renders correctly in both the current Sidebar and PreviewSidebar containers"
    - "All 264 existing tests pass unchanged"
  artifacts:
    - path: "src/components/Panels/MapControlsPanel.tsx"
      provides: "Layout-agnostic map view controls (SelectionInfo + GenerateButton)"
      exports: ["MapControlsPanel"]
    - path: "src/components/Panels/ModelControlsPanel.tsx"
      provides: "Layout-agnostic model view controls (Back to Edit, ModelSizeSection, layers, ExportPanel)"
      exports: ["ModelControlsPanel"]
    - path: "src/components/Panels/SidebarContent.tsx"
      provides: "View-driven content switcher reading showPreview from store"
      exports: ["SidebarContent"]
    - path: "src/components/Sidebar/Sidebar.tsx"
      provides: "Thin container shell rendering SidebarContent"
    - path: "src/components/Preview/PreviewSidebar.tsx"
      provides: "Thin container shell rendering ModelControlsPanel"
  key_links:
    - from: "src/components/Panels/SidebarContent.tsx"
      to: "src/store/mapStore.ts"
      via: "useMapStore(s => s.showPreview)"
      pattern: "showPreview.*ModelControlsPanel.*MapControlsPanel"
    - from: "src/components/Sidebar/Sidebar.tsx"
      to: "src/components/Panels/SidebarContent.tsx"
      via: "import { SidebarContent }"
      pattern: "import.*SidebarContent.*from.*Panels"
    - from: "src/components/Preview/PreviewSidebar.tsx"
      to: "src/components/Panels/ModelControlsPanel.tsx"
      via: "import { ModelControlsPanel }"
      pattern: "import.*ModelControlsPanel.*from.*Panels"
---

<objective>
Extract sidebar content into layout-agnostic panel components and wire a view-driven content switcher.

Purpose: Satisfy LAYOUT-03 (sidebar content switches between map controls and model controls based on active view) and prepare the foundation for Phase 17's persistent sidebar by creating a single `<SidebarContent />` insertion point that any container can render.

Output: Three new panel components in `src/components/Panels/`, updated `Sidebar.tsx` and `PreviewSidebar.tsx` as thin container shells.
</objective>

<execution_context>
@/Users/agrignon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/agrignon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-content-architecture/15-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/store/mapStore.ts (relevant selectors):
```typescript
showPreview: boolean;
setShowPreview: (show: boolean) => void;
bbox: { sw: { lat: number; lon: number }; ne: { lat: number; lon: number } } | null;
```

From src/components/Sidebar/Sidebar.tsx (current structure):
```typescript
// MobileSidebar and DesktopSidebar receive hasBbox prop — this moves into MapControlsPanel
function MobileSidebar({ hasBbox }: { hasBbox: boolean }) { ... }
function DesktopSidebar({ hasBbox }: { hasBbox: boolean }) { ... }
export function Sidebar() {
  const hasBbox = useMapStore((s) => s.bbox !== null);
  const tier = useBreakpoint();
  const isMobile = tier === 'mobile';
  // ...renders MobileSidebar or DesktopSidebar
}
```

From src/components/Preview/PreviewSidebar.tsx (current structure):
```typescript
// isOpen toggle + toggle button stay in PreviewSidebar
// Inner content (lines 72-119) moves to ModelControlsPanel
export function PreviewSidebar() {
  const [isOpen, setIsOpen] = useState(true);
  const setShowPreview = useMapStore((s) => s.setShowPreview);
  // ...toggle button + model controls content
}
```

From src/components/Sidebar/SelectionInfo.tsx:
```typescript
export function SelectionInfo() { ... }
// Stays at src/components/Sidebar/SelectionInfo.tsx — DO NOT move
```

From src/components/Sidebar/GenerateButton.tsx:
```typescript
export function GenerateButton() { ... }
export function triggerRegenerate(): Promise<void>;
// CRITICAL: Stays at src/components/Sidebar/GenerateButton.tsx — DO NOT move
// Test imports from '../GenerateButton' (relative to __tests__ dir)
```

From src/hooks/useBreakpoint.ts:
```typescript
export function useBreakpoint(): 'mobile' | 'tablet' | 'desktop';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create layout-agnostic panel components</name>
  <files>src/components/Panels/MapControlsPanel.tsx, src/components/Panels/ModelControlsPanel.tsx, src/components/Panels/SidebarContent.tsx</files>
  <action>
Create the `src/components/Panels/` directory and three new files.

**MapControlsPanel.tsx** — Extract map view content from Sidebar.tsx:
- Import `useMapStore` from `../../store/mapStore`
- Import `SelectionInfo` from `../Sidebar/SelectionInfo`
- Import `GenerateButton` from `../Sidebar/GenerateButton`
- Read `hasBbox` directly from store: `const hasBbox = useMapStore((s) => s.bbox !== null)`
- Render: outer flex column div with gap 8px, then `hasBbox ? <SelectionInfo /> : <p>` instruction text, then a divider, then `<GenerateButton />`
- For the instruction text: use the more complete desktop copy: "Search for a location, then tap **Draw Area** and drag on the map to select a region. On desktop you can also **Shift+drag**." — this works on all tiers
- Style: `fontSize: '13px'`, `color: 'rgba(255,255,255,0.5)'`, `lineHeight: 1.5`, `margin: 0`
- GenerateButton wrapper: `borderTop: '1px solid rgba(255,255,255,0.08)'`, `paddingTop: '6px'`
- ZERO container styles: no `position`, no `width`, no `maxHeight`, no `overflow`, no `backdropFilter`, no `boxShadow`, no `zIndex`

**ModelControlsPanel.tsx** — Extract model controls content from PreviewSidebar.tsx lines 72-119:
- Import `useMapStore` from `../../store/mapStore`
- Import all section components from `../Preview/`: `ModelSizeSection`, `TerrainSection`, `BuildingsSection`, `RoadsSection`, `WaterSection`, `VegetationSection`, `ExportPanel`
- Read `setShowPreview` from store: `const setShowPreview = useMapStore((s) => s.setShowPreview)`
- Render the exact same content currently inside the `isOpen && (` block of PreviewSidebar, specifically:
  - Header div with "Back to Edit" button + "Model Controls" span
  - "Back to Edit" button: `background: 'transparent'`, `border: 'none'`, `color: '#9ca3af'`, `fontSize: '12px'`, `padding: '4px 0'`, `cursor: 'pointer'`, `display: 'block'`, `width: '100%'`, `textAlign: 'left'` — include the `onMouseEnter`/`onMouseLeave` hover handlers that toggle color between `#9ca3af` and `#d1d5db`
  - ModelSizeSection in a div with `borderBottom: '1px solid rgba(255,255,255,0.08)'`, `paddingBottom: '12px'`, `paddingTop: '8px'`
  - "Layers" subheading: `color: '#6b7280'`, `fontSize: '11px'`, `fontWeight: 600`, `textTransform: 'uppercase'`, `letterSpacing: '0.05em'`
  - Layer sections in pipeline order: Terrain, Buildings, Roads, Water, Vegetation
  - Divider: `borderTop: '1px solid rgba(255,255,255,0.08)'`, `marginTop: '8px'`
  - ExportPanel
- ZERO container styles: no `position`, no `width`, no `maxHeight`, no `overflow`, no `backdropFilter`, no `boxShadow`, no `zIndex`, no `padding` on the outer element (padding is the container's job)

**SidebarContent.tsx** — View-driven content switcher:
- Import `useMapStore` from `../../store/mapStore`
- Import `MapControlsPanel` from `./MapControlsPanel`
- Import `ModelControlsPanel` from `./ModelControlsPanel`
- Read `showPreview` from store: `const showPreview = useMapStore((s) => s.showPreview)`
- Return: `showPreview ? <ModelControlsPanel /> : <MapControlsPanel />`
- This is the single insertion point. Conditional rendering (ternary), NOT `display: none`. Only one panel mounts at a time.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Three new files exist in src/components/Panels/, each exports its named component, TypeScript compiles with no errors</done>
</task>

<task type="auto">
  <name>Task 2: Update Sidebar.tsx and PreviewSidebar.tsx to be thin container shells</name>
  <files>src/components/Sidebar/Sidebar.tsx, src/components/Preview/PreviewSidebar.tsx</files>
  <action>
**Sidebar.tsx** — Replace inline content with `<SidebarContent />`:
- Add import: `import { SidebarContent } from '../Panels/SidebarContent';`
- Remove import of `SelectionInfo` (no longer used directly)
- Remove import of `GenerateButton` (no longer used directly)
- Remove the `hasBbox` prop from both `MobileSidebar` and `DesktopSidebar` — they no longer need it
- Remove the `hasBbox` store read from the `Sidebar()` function — no longer needed
- **MobileSidebar**: Keep all container styles (position, backdrop, safe area, maxHeight). Replace the content area (the `hasBbox &&` blocks and the GenerateButton wrapper on lines 34-50) with a single scrollable content div containing `<SidebarContent />`. The content area: `<div style={{ padding: '0 14px 6px', overflowY: 'auto', flexShrink: 1, minHeight: 0 }}><SidebarContent /></div>`. Keep the "MapMaker" header div at the top.
- **DesktopSidebar**: Keep all container styles (position, width, backdrop, border). Replace the content area (lines 81-93) with `<SidebarContent />` inside the existing flex-1 scrollable div: `<div style={{ padding: '12px 14px', flex: 1, overflowY: 'auto' }}><SidebarContent /></div>`. Remove the separate GenerateButton wrapper div (lines 91-93) — GenerateButton is now inside MapControlsPanel. Keep the "MapMaker" `<h1>` header at the top.
- The `Sidebar()` function still reads `tier` from `useBreakpoint()` and renders `MobileSidebar` or `DesktopSidebar` based on tier — that logic stays unchanged.

**PreviewSidebar.tsx** — Replace inline content with `<ModelControlsPanel />`:
- Add import: `import { ModelControlsPanel } from '../Panels/ModelControlsPanel';`
- Remove imports of `ModelSizeSection`, `TerrainSection`, `BuildingsSection`, `RoadsSection`, `WaterSection`, `VegetationSection`, `ExportPanel` (all now inside ModelControlsPanel)
- Remove the `setShowPreview` store read from `PreviewSidebar()` — `ModelControlsPanel` handles it internally
- Keep the `[isOpen, setIsOpen]` state and the toggle button — these are container concerns
- Replace the inner content of the `isOpen && (` block (lines 72-119 — everything inside the panel container div) with just `<ModelControlsPanel />`
- Keep the panel container div's styles: `pointerEvents: 'auto'`, `width: 'min(240px, calc(100vw - 44px))'`, `maxHeight: '100%'`, `backgroundColor`, `backdropFilter`, `borderLeft`, `boxShadow`, `padding: '14px'`, `overflowY: 'auto'`, `display: 'flex'`, `flexDirection: 'column'`
- The `useState` import can stay (still used for `isOpen`)

CRITICAL: Do NOT move or rename `GenerateButton.tsx` or `SelectionInfo.tsx`. They stay at their current paths. The test at `src/components/Sidebar/__tests__/GenerateButton.test.ts` imports `triggerRegenerate` from `../GenerateButton` — this path MUST remain valid.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -15</automated>
  </verify>
  <done>Sidebar.tsx renders SidebarContent instead of inline content, PreviewSidebar.tsx renders ModelControlsPanel instead of inline content, all 264 tests pass, npx tsc --noEmit succeeds</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — TypeScript compiles with zero errors
2. `npx vitest run` — All 264 tests pass (21 files)
3. `npx vite build` — Production build succeeds
4. Manual: Open app in browser, verify map controls appear in sidebar on map view
5. Manual: Generate a model, verify model controls (layers, export) appear in PreviewSidebar
6. Manual: Click "Back to Edit" in model controls, verify sidebar switches back to map controls
</verification>

<success_criteria>
- Three new files exist: `src/components/Panels/{MapControlsPanel,ModelControlsPanel,SidebarContent}.tsx`
- `Sidebar.tsx` no longer contains `SelectionInfo` or `GenerateButton` imports — uses `SidebarContent`
- `PreviewSidebar.tsx` no longer contains layer section imports — uses `ModelControlsPanel`
- `SidebarContent` reads `showPreview` from store and conditionally renders the correct panel
- Panel components contain ZERO container/positional styles (no position, width, maxHeight, overflow, backdrop, shadow, zIndex)
- `GenerateButton.tsx` and `SelectionInfo.tsx` remain at original paths (test import preservation)
- All 264 tests pass unchanged
- TypeScript compiles with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/15-content-architecture/15-01-SUMMARY.md`
</output>
