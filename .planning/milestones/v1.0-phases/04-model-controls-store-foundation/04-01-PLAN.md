---
phase: 04-model-controls-store-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/mapStore.ts
  - src/components/Preview/BuildingMesh.tsx
  - src/components/Preview/ExportPanel.tsx
autonomous: true
requirements:
  - CTRL-01
  - CTRL-02
  - CTRL-03

must_haves:
  truths:
    - "Zustand store has layerToggles, units, and targetHeightMM fields with correct defaults"
    - "Toggling buildings off in the store immediately hides building geometry in the 3D preview without regeneration"
    - "Export pipeline excludes building geometry when layerToggles.buildings is false"
    - "setTargetWidth action auto-calculates targetDepthMM from bbox aspect ratio"
  artifacts:
    - path: "src/store/mapStore.ts"
      provides: "Extended store with layerToggles, units, targetHeightMM, setLayerToggle, setUnits, setTargetWidth, setTargetHeightMM"
      contains: "layerToggles"
    - path: "src/components/Preview/BuildingMesh.tsx"
      provides: "Building mesh visibility wired to layerToggles.buildings"
      contains: "visible"
    - path: "src/components/Preview/ExportPanel.tsx"
      provides: "Export gated on layerToggles.buildings"
      contains: "layerToggles"
  key_links:
    - from: "src/components/Preview/BuildingMesh.tsx"
      to: "src/store/mapStore.ts"
      via: "useMapStore selector for layerToggles.buildings"
      pattern: "useMapStore.*layerToggles\\.buildings"
    - from: "src/components/Preview/ExportPanel.tsx"
      to: "src/store/mapStore.ts"
      via: "useMapStore selector for layerToggles.buildings to gate building export"
      pattern: "layerToggles.*buildings"
---

<objective>
Extend the Zustand store with all new state fields needed for Phase 4 (layer toggles, units, Z height) and wire the existing 3D mesh components to respect layer visibility. This is the data foundation that the UI plan (Plan 02) will build on top of.

Purpose: Every subsequent component — the new sidebar sections, the export pipeline, and future phase mesh components — reads from these store fields. Getting the store contract right first means the UI plan can focus purely on rendering.

Output: Extended mapStore.ts with 3 new state fields + 4 new actions, BuildingMesh with visible prop, ExportPanel gated on layer toggle.
</objective>

<execution_context>
@/Users/agrignon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/agrignon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-model-controls-store-foundation/04-RESEARCH.md
@src/store/mapStore.ts
@src/components/Preview/BuildingMesh.tsx
@src/components/Preview/ExportPanel.tsx
@src/types/geo.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Zustand store with layer toggles, units, and dimension fields</name>
  <files>src/store/mapStore.ts</files>
  <action>
Add the following to MapState interface:

1. `layerToggles` field — typed as `{ buildings: boolean; roads: boolean; water: boolean; vegetation: boolean }`. Define a `LayerToggles` interface above `MapState` for reuse. NOTE: Terrain has NO toggle per user decision (always on). Default all values to `true`.

2. `units` field — typed as `'mm' | 'in'`. Default: `'mm'`.

3. `targetHeightMM` field — typed as `number`. Default: `0` (0 means auto-calculate from elevation range). This is the Z height override.

Add the following to MapActions interface:

4. `setLayerToggle: (layer: keyof LayerToggles, enabled: boolean) => void` — uses functional set with spread: `set((state) => ({ layerToggles: { ...state.layerToggles, [layer]: enabled } }))`.

5. `setUnits: (units: 'mm' | 'in') => void` — simple `set({ units })`. Store always holds mm values; unit is display-only. No value conversion happens on switch.

6. `setTargetWidth: (widthMM: number) => void` — auto-calculates `targetDepthMM` from bbox aspect ratio: `set((state) => { if (!state.dimensions) return { targetWidthMM: widthMM }; const aspectRatio = state.dimensions.heightM / state.dimensions.widthM; return { targetWidthMM: widthMM, targetDepthMM: widthMM * aspectRatio }; })`. Uses `get` pattern — Zustand 5 `create<T>()((set, get) => ...)`.

7. `setTargetHeightMM: (value: number) => void` — simple `set({ targetHeightMM: value })`.

Export the `LayerToggles` type so Plan 02 UI components can import it.

IMPORTANT: Keep the existing `setTargetDimensions(widthMM, depthMM)` action intact — it's still used by ExportPanel until Plan 02 migrates dimension inputs to ModelSizeSection. Do NOT remove it.

IMPORTANT: The `create<MapStore>()` call must use `(set, get)` since `setTargetWidth` needs `get()` to read current `dimensions`. Update the create call signature from `create<MapStore>((set) => ({` to `create<MapStore>((set, get) => ({`.
  </action>
  <verify>
    <automated>cd /Users/agrignon/projects/MapMaker && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Confirm mapStore.ts has layerToggles, units, targetHeightMM in MapState, and setLayerToggle, setUnits, setTargetWidth, setTargetHeightMM in MapActions</manual>
  </verify>
  <done>MapState has layerToggles (4 booleans, all default true), units (default 'mm'), targetHeightMM (default 0). MapActions has setLayerToggle, setUnits, setTargetWidth (auto-calculates depth), setTargetHeightMM. LayerToggles type is exported. Existing actions untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Wire mesh visibility and gate export on layer toggles</name>
  <files>src/components/Preview/BuildingMesh.tsx, src/components/Preview/ExportPanel.tsx</files>
  <action>
**BuildingMesh.tsx:**

1. Add a new store selector: `const buildingsVisible = useMapStore((s) => s.layerToggles.buildings);`

2. Add `visible={buildingsVisible}` prop to the `<mesh>` element in the return JSX. This uses Three.js `mesh.visible` — when false, the mesh is not rendered but geometry stays in memory. Toggle back on is instant (no regeneration needed). Per user decision: "Toggling a layer off instantly hides its geometry in the Three.js scene (no regeneration)".

3. Do NOT change the early return guards (`if (buildingGenerationStatus !== 'ready') return null`). The `visible` prop only applies when the mesh is actually rendered — it controls whether Three.js draws it, not whether the React component mounts.

**ExportPanel.tsx:**

4. Add store selector: `const buildingsVisible = useMapStore((s) => s.layerToggles.buildings);`

5. Modify the `hasBuildings` check in `handleExport()` to also check the toggle:
   Change: `const hasBuildings = Boolean(buildingFeatures && buildingFeatures.length > 0 && utmZone);`
   To: `const hasBuildings = Boolean(buildingFeatures && buildingFeatures.length > 0 && utmZone && buildingsVisible);`

   This ensures "Export matches the preview — toggled-off layers are excluded from STL" per user decision.

6. Also update the `generateFilename` call to pass `hasBuildings` which now reflects the toggle state — if buildings are toggled off, the filename drops the `-buildings` suffix.

NOTE: Do NOT touch the dimension inputs in ExportPanel yet. Plan 02 will migrate them to ModelSizeSection and simplify ExportPanel.
  </action>
  <verify>
    <automated>cd /Users/agrignon/projects/MapMaker && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Open app, generate terrain+buildings, toggle buildings off in store (via React DevTools or code) — buildings disappear from preview. Export STL with buildings off — exported file has no building geometry and filename has no -buildings suffix.</manual>
  </verify>
  <done>BuildingMesh has visible={buildingsVisible} bound to layerToggles.buildings from store. ExportPanel gates building inclusion on buildingsVisible — toggled-off buildings are excluded from STL export and filename. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors related to new store fields or component changes
2. mapStore.ts exports LayerToggles type and has all 4 new actions
3. BuildingMesh.tsx reads layerToggles.buildings and passes it as visible prop to mesh
4. ExportPanel.tsx checks buildingsVisible before including buildings in export
5. All existing tests pass: `npx vitest run`
</verification>

<success_criteria>
- Store has layerToggles {buildings, roads, water, vegetation} all defaulting to true
- Store has units defaulting to 'mm' and targetHeightMM defaulting to 0
- setTargetWidth auto-calculates depth from aspect ratio
- BuildingMesh visibility controlled by layerToggles.buildings
- Export pipeline respects layer toggles for building inclusion
- No TypeScript errors, all existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-model-controls-store-foundation/04-01-SUMMARY.md`
</output>
