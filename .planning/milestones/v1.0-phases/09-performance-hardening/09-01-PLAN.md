---
phase: 09-performance-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/martini.d.ts
  - src/components/Preview/PreviewControls.tsx
  - src/components/Preview/ExportPanel.tsx
  - src/components/Preview/BuildingsSection.tsx
  - src/lib/mesh/solid.ts
  - src/lib/mesh/terrain.ts
  - src/lib/mesh/terrainRaycaster.ts
  - src/lib/buildings/__tests__/walls.test.ts
  - src/lib/roads/__tests__/parse.test.ts
  - src/lib/water/__tests__/parse.test.ts
autonomous: true
requirements:
  - FNDN-04

must_haves:
  truths:
    - "`npx tsc -b` exits with code 0 and produces no error output"
    - "`npx vite build` completes successfully with a production bundle"
    - "All 179+ existing tests still pass after changes"
  artifacts:
    - path: "src/types/martini.d.ts"
      provides: "Type declarations for @mapbox/martini"
      contains: "declare module '@mapbox/martini'"
  key_links:
    - from: "src/lib/mesh/terrain.ts"
      to: "src/types/martini.d.ts"
      via: "Module resolution"
      pattern: "import.*@mapbox/martini"
---

<objective>
Fix all 33 TypeScript build errors so `tsc -b` passes clean, making the production build deployable.

Purpose: FNDN-04 requires `npm run build` to compile without TypeScript errors. Currently `tsc -b` reports 33 errors across 9 files. All are mechanical fixes — unused variables, a missing module declaration, a type version conflict, a wrong enum variant, and test helper typing issues. No architecture changes needed.

Output: Clean `tsc -b` with zero errors; all existing tests still passing.
</objective>

<execution_context>
@/Users/agrignon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/agrignon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/09-performance-hardening/09-RESEARCH.md

<interfaces>
<!-- Error categories from `tsc -b` output (33 errors, 9 files): -->

<!-- Category 1: Missing module declaration (1 error) -->
<!-- src/lib/mesh/terrain.ts(7,21): TS7016 — @mapbox/martini has no type declarations -->

<!-- Category 2: Unused variables / TS6133 + TS6199 (12 errors) -->
<!-- src/components/Preview/PreviewControls.tsx(2,1): THREE import unused -->
<!-- src/components/Preview/ExportPanel.tsx(93,9): roadStyle unused -->
<!-- src/lib/mesh/solid.ts(108-111,153): minX, maxX, minY, maxY, n unused -->
<!-- src/lib/mesh/terrain.ts(141,33+47)(142,47): minElevation, maxElevation, geographicDepthM unused -->
<!-- src/lib/buildings/__tests__/walls.test.ts(131,5): all variables unused -->

<!-- Category 3: Type mismatch (1 error) -->
<!-- src/lib/mesh/terrainRaycaster.ts(28,3): TS2322 — MeshBVH version conflict between 0.9.8 and drei's nested 0.8.3 -->

<!-- Category 4: Wrong enum variant (1 error) -->
<!-- src/components/Preview/BuildingsSection.tsx(11,7): TS2367 — 'loading' not in BuildingGenerationStatus union -->

<!-- Category 5: Test FeatureCollection typing (19 errors) -->
<!-- src/lib/roads/__tests__/parse.test.ts: 13 errors — makeFeatureCollection returns object[] not Feature[] -->
<!-- src/lib/water/__tests__/parse.test.ts: 7 errors — same issue -->

From src/types/geo.ts:
```typescript
export type BuildingGenerationStatus = 'idle' | 'fetching' | 'building' | 'ready' | 'error';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix production code TypeScript errors (14 errors across 7 files)</name>
  <files>src/types/martini.d.ts, src/components/Preview/PreviewControls.tsx, src/components/Preview/ExportPanel.tsx, src/components/Preview/BuildingsSection.tsx, src/lib/mesh/solid.ts, src/lib/mesh/terrain.ts, src/lib/mesh/terrainRaycaster.ts</files>
  <action>
Fix all 14 TypeScript errors in production source files. All fixes are mechanical — no behavioral changes.

**1. Create `src/types/martini.d.ts`** — type declaration for `@mapbox/martini`:
```typescript
declare module '@mapbox/martini' {
  export default class Martini {
    constructor(gridSize: number);
    createTile(terrain: Float32Array): {
      getMesh(maxError: number): {
        vertices: Uint16Array;
        triangles: Uint32Array;
      };
    };
  }
}
```

**2. `src/components/Preview/PreviewControls.tsx`** — remove the unused `import * as THREE from 'three'` on line 2.

**3. `src/components/Preview/ExportPanel.tsx`** — remove `roadStyle` from the destructuring on line 93 (`const roadStyle = useMapStore((s) => s.roadStyle);`). The variable was used previously but `roadStyle` is now hardcoded to `'raised'` inside `handleExport` on line 245, so this store subscription is dead code.

**4. `src/components/Preview/BuildingsSection.tsx`** — change `buildingGenerationStatus === 'loading'` on line 11 to `buildingGenerationStatus === 'fetching'`. The `BuildingGenerationStatus` union type has no `'loading'` variant; `'fetching'` is the correct in-progress status during OSM data fetch.

**5. `src/lib/mesh/solid.ts`** — remove the 5 unused variables:
- Lines 108-111: Remove `const minX = bbox.min.x;`, `const maxX = bbox.max.x;`, `const minY = bbox.min.y;`, `const maxY = bbox.max.y;` — the code later accesses `bbox.min.x` etc. directly (or via `perimeterLoop`), so these locals are never read.
- Line 153: Remove `const n = perimeterLoop.length;` — `perimeterLoop.length` is used directly in subsequent code.

**6. `src/lib/mesh/terrain.ts`** — fix the destructuring on lines 141-142:
- Line 141: Change `const { gridSize, elevations, minElevation, maxElevation } = elevationData;` to `const { gridSize, elevations } = elevationData;` (minElevation and maxElevation are unused — smoothedMin/smoothedMax computed on lines 149-154 are used instead).
- Line 142: Remove `geographicDepthM` from the destructuring. Change to: `const { widthMM, depthMM, geographicWidthM, exaggeration, minHeightMM, maxError, targetReliefMM } = params;`

**7. `src/lib/mesh/terrainRaycaster.ts`** — add type assertion on line 28:
Change `tempMesh.geometry.boundsTree = bvh;` to `tempMesh.geometry.boundsTree = bvh as any;`
This resolves the structural type mismatch between `three-mesh-bvh@0.9.8` (project root) and `@react-three/drei`'s nested `three-mesh-bvh@0.8.3`. At runtime only 0.9.8 is loaded; the conflict is type-only. Add a comment above: `// Type assertion: resolves version mismatch between three-mesh-bvh 0.9.8 and drei's nested 0.8.3`
  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | grep -c "error TS" | grep -q "^19$" && echo "14 prod errors fixed, 19 test errors remain" || npx tsc -b 2>&1 | tail -5</automated>
  </verify>
  <done>All 14 production code TypeScript errors resolved. Only 19 test file errors remain (addressed in Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Fix test file TypeScript errors and verify clean build (19 errors across 3 files)</name>
  <files>src/lib/roads/__tests__/parse.test.ts, src/lib/water/__tests__/parse.test.ts, src/lib/buildings/__tests__/walls.test.ts</files>
  <action>
Fix all 19 TypeScript errors in test files. All fixes are typing-only — no test behavior changes.

**1. `src/lib/roads/__tests__/parse.test.ts`** (13 errors) — fix the `makeFeatureCollection` helper function. The current signature accepts `object[]` and returns `{ type: "FeatureCollection"; features: object[] }`, which is not assignable to `FeatureCollection<Geometry, GeoJsonProperties>`.

Fix: Add imports and type the helper properly:
```typescript
import type { Feature, FeatureCollection } from 'geojson';
```
Change the helper to:
```typescript
function makeFeatureCollection(features: Feature[]): FeatureCollection {
  return { type: 'FeatureCollection' as const, features };
}
```
Also ensure the individual feature factory functions (`makeLineStringFeature`, etc.) have `as const` on their `type: 'Feature'` return values so they satisfy the `Feature` type. Check each factory — they likely already do this but verify.

**2. `src/lib/water/__tests__/parse.test.ts`** (7 errors, including line 213) — apply the same `makeFeatureCollection` helper fix as above. Import `Feature` and `FeatureCollection` from `geojson`. Type the helper's parameter as `Feature[]` and return type as `FeatureCollection`.

**3. `src/lib/buildings/__tests__/walls.test.ts`** (1 error, line 131, TS6199) — find the unused variable(s) on or near line 131 and either remove them or prefix with `_`. The error is "All variables are unused" which typically means a destructuring assignment where none of the destructured variables are read. Prefix unused ones with `_` (e.g., `const _foo = ...` or `const { _bar } = ...`).

After all fixes:
- Run `npx tsc -b` — must exit 0 with no output
- Run `npx vitest run` — all tests must pass (179+)
- Run `npx vite build` — must succeed with production bundle
  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | grep -c "error" | grep -q "^0$" && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>`tsc -b` produces zero errors. `vite build` completes successfully. All existing tests pass. FNDN-04 is satisfied.</done>
</task>

</tasks>

<verification>
1. `npx tsc -b` exits with code 0 and produces no error output
2. `npx vite build` completes successfully, producing `dist/` output
3. `npx vitest run` — all 179+ tests pass with no regressions
4. No `// @ts-ignore` or `// @ts-expect-error` suppressions used (genuine fixes only, except the one `as any` for BVH version mismatch which is documented)
</verification>

<success_criteria>
- `tsc -b` reports 0 errors (currently 33)
- Production build (`npm run build` / `npx vite build`) succeeds
- All existing tests pass unchanged
- Worker bundle loads correctly in production build output
</success_criteria>

<output>
After completion, create `.planning/phases/09-performance-hardening/09-01-SUMMARY.md`
</output>
